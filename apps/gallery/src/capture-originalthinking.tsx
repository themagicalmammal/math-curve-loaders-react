import { createRoot } from "react-dom/client";
import { OriginalThinking } from "@math-curve-loaders/react";

createRoot(document.getElementById("root")!).render(
  <OriginalThinking
    style={{ width: 160, height: 160, color: "#6366f1" }}
  />
);
